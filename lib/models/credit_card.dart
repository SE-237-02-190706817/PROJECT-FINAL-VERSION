class CreditCard {
  String cardNumber, expirydate, cvv, cardHolder, id;
  CreditCard(
      {required this.cardNumber,
      required this.expirydate,
      required this.cvv,
      required this.cardHolder,
      required this.id});
}
